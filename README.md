# CanvasMap

This is an old experiment in building a canvas based map system that was built off of a series of image tiles. A series of hypermedia driven API endpoints would allow a dumb client to easily build out a map of arbitrary size.
